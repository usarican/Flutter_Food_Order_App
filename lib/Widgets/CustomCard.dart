import 'package:flutter/material.dart';

class YemekItem extends StatefulWidget {
  const YemekItem({Key? key}) : super(key: key);

  @override
  State<YemekItem> createState() => _YemekItemState();
}

class _YemekItemState extends State<YemekItem> {
  @override
  Widget build(BuildContext context) {
    return Card(

    );
  }
}
